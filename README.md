# Diffusion_Model
A PyTorch implementation of a basic Diffusion Model (DDPM) trained on the Stanford Cars dataset (from Hugging Face). This project demonstrates how diffusion models generate realistic images by gradually denoising random noise through forward and reverse processes.
